<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-9">
            <nav class="navbar bg-light">
                <form class="container-fluid justify-content-start">
                    <a href="{{route('admin.concert.index')}}" class="btn btn-outline-primary me-2" type="button">Konser</a>
                    <a href="{{route('admin.ticket.index')}}" class="btn btn-outline-primary me-2" type="button">Tiket</a>
                    <a href="{{route('admin.ticket.checkin')}}" class="btn btn-outline-primary me-2" type="button">Check In</a>
                </form>
            </nav>
        </div>
    </div>
</div>
